# Codex Project Rules

## 適用範囲

- このルールは、Codex がこのプロジェクト内で作業する際に適用する。
- PR、Linear issue、コミットはすべて日本語で行う。
- Linear issue のタイトル、本文、コメント、操作時の説明は日本語で作成する。
- Codex の PR レビュー出力は、GitHub 上の `@codex review` による自動レビューを含めて日本語で作成する。
- Linear MCP や GitHub MCP を使用して issue や PR を操作した場合は、操作内容を最終報告または PR コメントに残す。

## 作業開始時

- 作業開始前に、対象 Linear issue の内容を確認する。
- 作業開始前に、現在のブランチ、未コミット差分、未追跡ファイルを確認する。
- 既存の未コミット差分がある場合は、ユーザー作業の可能性があるため勝手に戻さない。
- 実装前に影響範囲を確認し、不要なリファクタリングを避ける。

## Linear MCP 操作

- Linear issue の確認、作成、更新は Linear MCP を使用する。
- 作業対象 issue は、作業開始時に Linear MCP でタイトル、本文、ステータス、担当者、ラベル、プロジェクトを確認する。
- 作業を開始する場合は、必要に応じて対象 issue のステータスを `In Progress` に更新する。
- 作業が完了した場合は、必要に応じて対象 issue のステータスを `Done` に更新する。
- issue を新規作成する場合は、チーム `Mytimes` に作成し、タイトル、概要、対応内容、完了条件を日本語で記載する。
- issue の作成・更新時は、重複を避けるため、事前に Linear MCP で既存 issue を検索する。
- Linear MCP で issue を操作した場合は、操作した issue 番号、変更内容、ステータス変更を最終報告または PR コメントに残す。

## ブランチ

- ブランチ名は Linear の issue 番号と同じ形式にする。
- 例: `MYT-19`

## コミット

- コミットメッセージは日本語で作成する。
- コミットメッセージは命令動詞を使用する。
- コミットメッセージは 50 文字前後を目安にする。
- コミットメッセージの末尾に、対応する Linear issue 番号を `(<Linear issue番号>)` の形式で付ける。
- コミットメッセージでは、変数名やこのプロジェクト固有の内部用語をできるだけ避ける。
- コミットメッセージは、機能名と変更内容が分かる表現にする。
- 指示に含まれていない場合でも、作業単位ごとにこまめにコミットする。

## テスト

- 変更後は、関連するテスト、lint、型チェックのうち実行可能なものを実行する。
- push 前には必ずテストを実行する。
- テストを実行できなかった場合は、その理由を PR 本文またはコメントに明記する。
- push 後、テスト結果を PR にコメントする。

## Pull Request

- PR のタイトルと本文は日本語で作成する。
- PR を作成する前に、リポジトリに PR テンプレートがあるか確認する。
- PR テンプレートがある場合は、その構成に従う。
- PR テンプレートがない場合は、少なくとも次の項目を本文に含める。
  - `## 概要`
  - `## 確認内容`
  - 関連 Linear issue がある場合は `Linear: <Linear issue番号>`
- `## 概要` には、実装・修正した内容を箇条書きで簡潔に書く。
- `## 確認内容` には、実際に実行したコマンドと確認した動作を書く。
- PR 本文には、関連 Linear issue を `Linear: <Linear issue番号>` 形式で記載する。
- PR 作成前に、未コミットの差分と push 済みブランチを確認する。
- PR は作業ブランチから `main` 向けに作成する。

## レビューコメント

- PR レビュー、レビューコメント、サマリーコメントはすべて日本語で作成する。
- `@codex review` による自動レビューでも、日本語で回答する。
- 指摘は `問題`、`影響`、`対応案` が分かる形式で書く。
- 優先度表記は `High` / `Medium` / `Low` を使う。
- `Severity` ではなく `優先度` と表記する。
- 英語の定型文は必要最小限にし、説明本文は日本語にする。
- 指摘がない場合は「優先して対応すべき指摘はありませんでした」と日本語でコメントする。

## 安全な作業

- 依存関係の追加・更新は、必要性を説明できる場合に限る。
- 生成物、ビルド成果物、環境依存ファイルは原則コミットしない。
- 秘密情報や個人情報をコミットしない。
- 破壊的な git 操作はユーザーの明示的な許可なしに実行しない。
